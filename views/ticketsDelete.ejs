
<% if(locals.user.role=='admin'){ %>

    <table id="ticketsTable" class="moviestable table-striped table-bordered ticketsTable">
        <tr>
          <th>Film name</th>
          <th>Film cast </th>
          <th>Film price </th>
          <!-- <th>delete </th> -->
          <th>update </th>
        </tr>
        
        <tr v-for="ticket in tickets">

         
            <td>
              <div class="control-group">
                <div class="mb-0 form-floating controls pb-2"><input :form="`update${ticket._id}`" :value="ticket.name" type="name" name="name" required
                   style="width: 306px;">
                  </div>
              </div>
            </td>
          <td>
            <div class="control-group">
              <div class="mb-0 form-floating controls pb-2"><input :form="`update${ticket._id}`" :value="ticket.cast" type="name" name="cast" required
                 style="width: 306px;">
                </div>
            </div>
          </td>
          <td>
            <div class="control-group">
              <div class="mb-0 form-floating controls pb-2"><input :form="`update${ticket._id}`" :value="ticket.price" type="number" name="price" required
                 style="width: 306px;">
                </div>
            </div>
          </td>
      <td>
        <form :action="`tickets/delete/${ticket._id}`" method="post">
          <button type='submit' class="nupp">Delete</button>
        </form>

            <div id="success"></div>
            <div>
              <form :action="`tickets/update/${ticket._id}`" method="POST" :id="`update${ticket._id}`">
                <button class="btn btn-primary btn-xl" id="sendMessageButton" type="submit"
                style="width: 306px;">Update</button>    </form>
            </div>

          </td>

      
        </td> 
        </tr>
      
      </table>
    <% } else{ %>  
      <p>ei ole admin</p>
   <% } %>